- **ci(docker):** publish this fork's own container image — `.github/workflows/arcbyte-image.yml` builds `runner-base` from `master` for amd64 and arm64 on GitHub's hosted runners (webpack, 12 GB heap, swap as OOM headroom) and pushes `ghcr.io/arcbyteofficial/openai:latest` plus `sha-<commit>`. `docker-compose.selfhost.yml` now runs that image with `pull_policy: always` instead of the upstream `diegosouzapw/omniroute` image, which never carried this fork's UI or branding.
