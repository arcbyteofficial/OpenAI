- **feat(dashboard):** minimal neutral redesign of the whole UI (Apple / Linear / Vercel direction) — grayscale light/dark palette, monochrome primary actions with a single blue accent, self-hosted Geist + Geist Mono typography, 10px/6px radius scale, border-first surfaces without gradients, glows or the graph-paper wallpaper, a Linear-style sidebar and header, and every dashboard, auth and error page swept onto the new tokens. The Appearance accent picker now defaults to the theme-aware blue; a stored "coral" accent (the old default) migrates to it automatically.
- **feat(branding):** rebrand the UI to **ArcByte | Open AI** — theme-aware ArcByte logo served from `cdn.arcbyte.co` (`favicon_white.png` on light, `favicon_dark.png` on dark) in the sidebar, login, landing, docs navigation and home topology; the name in page titles, the PWA manifest (now in English) and the brand-only strings of all 66 locales; theme-matched favicons. Per-instance whitelabel settings (Appearance → app name / logo / favicon) still override the defaults.
- **feat(branding):** hide the upstream OmniRoute promotions — the home-page partner banners (Kimi, Cheaper Inference, VS Code extension), the remote news feed and the provider-card "Open Source Friend" chips — behind one `BRAND.showUpstreamPromotions` switch (off), and rename the "OmniProxy" sidebar section to "ArcByte Proxy" in every locale.
